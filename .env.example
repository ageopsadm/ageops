# Copie para .env e preencha (o arquivo .env está no .gitignore).
# Chave secreta Pagar.me — Edge Functions / Supabase secrets (nunca no HTML).
PAGARME_API_KEY=
# Encryption key pública (ek_live_…) — vai manualmente em age-ops-v4.html → PAGARME_EK.
PAGARME_EK=
